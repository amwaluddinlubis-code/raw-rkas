<div data-livewire-overview="true" data-panel="overview" class="db-panel space-y-4">
    @if($available)
        <div class="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><article class="db-metric-card"><p class="db-eyebrow">Database sekolah</p><p class="db-metric-value">Aktif</p><p class="db-metric-hint">Database tenant sedang digunakan.</p></article><article class="db-metric-card"><p class="db-eyebrow">Integritas</p><p class="db-metric-value {{ strtolower($integrity) === 'ok' ? 'text-emerald-600' : 'text-rose-600' }}">{{ strtoupper($integrity) }}</p><p class="db-metric-hint">Hasil pemeriksaan database.</p></article><article class="db-metric-card"><p class="db-eyebrow">Penyimpanan</p><p class="db-metric-value">{{ $totalStorage }}</p><p class="db-metric-hint">Akumulasi DB, WAL, dan SHM.</p></article><article class="db-metric-card"><p class="db-eyebrow">Migrasi terakhir</p><p class="db-metric-value">{{ $lastMigrated }}</p><p class="db-metric-hint">Untuk database sekolah aktif.</p></article></div>
        <div class="grid gap-4 xl:grid-cols-[1.15fr_.85fr]"><section class="db-card"><div class="db-card-header"><div><p class="db-eyebrow">Data utama</p><h2 class="db-card-title">Ringkasan jumlah record</h2></div></div><div class="db-table-counts">@forelse($tableCounts as $name => $count)<div><span class="font-mono">{{ $name }}</span><strong>{{ $count ?? 'error' }}</strong></div>@empty<p class="p-4 text-sm text-[var(--ui-fg-muted)]">Belum ada table count.</p>@endforelse</div></section><section class="db-card"><div class="db-card-header"><div><p class="db-eyebrow">Aksi cepat</p><h2 class="db-card-title">Tindakan penting</h2></div></div><div class="grid gap-3 p-4"><a href="#maintenance" class="db-action-card"><strong>Buka maintenance</strong><span>Checkpoint, migrasi, provision, dan optimasi database.</span></a><a href="{{ route('school-backups.index') }}" class="db-action-card"><strong>Backup sebelum perubahan</strong><span>Buat backup sebelum tindakan struktural.</span></a></div></section></div>
    @else
        <div class="db-empty-state">Tidak ada database aktif untuk diringkas.</div>
    @endif
</div>
